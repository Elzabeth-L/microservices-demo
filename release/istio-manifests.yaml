# Deprecated for this AKS deployment.
#
# This release uses either:
# - routes.yaml for kgateway Gateway API routing
# - agic-ingress.yaml for Azure Application Gateway Ingress Controller routing
#
# Keep this file empty of Kubernetes resources so applying the release directory
# does not create duplicate Gateway/HTTPRoute objects.
